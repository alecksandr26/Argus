import PageStub from './PageStub'

export default function LiveOps() {
  return (
    <PageStub
      title="Operación en vivo"
      description="Torre de Control — mapa de flota en tiempo real + cola de alertas (Route/Status_Route/Alert)."
    />
  )
}
